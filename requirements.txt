# ── Core ───────────────────────────────────────
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# ── Dashboard ──────────────────────────────────
streamlit>=1.35,<2.0

# ── LLM Clients ────────────────────────────────
openai>=1.30,<2.0

# ── Dev / Testing ──────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0

# ── Compliance RAG (Phase 8.2, OPTIONAL) ─────────
# Vector-store backend for compliance retrieval. rag.py uses it when present
# and falls back to a deterministic keyword-overlap retriever otherwise, so
# the app works without it. Install with: pip install -e .[rag]
# NOTE: on Windows the chroma-hnswlib wheel needs the C++ build tools; the
# optional fallback keeps the full pipeline working if you skip it.
# chromadb>=0.5,<1.0
