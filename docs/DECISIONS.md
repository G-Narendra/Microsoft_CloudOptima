# Architecture Decision Log

> Record major decisions we made, so the team has context on why things were done a certain way.

| Date | Decision | Rationale | Decided By |
|------|----------|-----------|------------|
| Jul 2026 | Continue from Narendra's existing CloudOptima architecture rather than starting from scratch | Already had a proven multi-agent design; team would rebuild implementation phase-by-phase | Team + Punit Shah |
| Jul 2026 | `dev` branch is the working branch; `main` only receives reviewed, merged phases | Keeps stable history reviewable and prevents broken builds on main | Narendra |
| Jul 2026 | Python 3.11+, Pydantic v2, Streamlit, Mock/Nvidia/Azure LLM backends | Fast AI-native stack with a free mock path for demos and tests | Team |
| Jul 2026 | Every text field auto-strips null bytes; `extra="forbid"` on all models | LLM and user output must never smuggle unvalidated fields or control chars | Phase 1 |
| Jul 2026 | Single `BaseAgent` template method with delimiters + injection guard + caching + error turns | Security and caching must live in one place so subclasses cannot forget them | Phase 4 |
| Jul 2026 | Compliance agent embeds the 21 rules hardcoded in the prompt AND validation | The LLM must never invent, drop, or modify rules (AI-poisoning defense) | Phase 5 |
| Jul 2026 | Judge may override any recommendation but can never disable security controls | Hard safety invariant on the final output | Phase 5 |
| Jul 2026 | Deterministic 6-pair conflict detection keyed per pair | v1 bug: budget conflicts fired for every pair, tripling duplicates | Phase 6 |
| Jul 2026 | 4 artifacts generated deterministically; IaC malware-scanned before storage | Dashboard always renders a complete result set; exec/eval never reaches the UI | Phase 6 |
| Aug 2026 | Dashboard runs the pipeline in a background thread and polls `agent_turns` | Real progress bar — advances only when a turn actually finishes, never faked | Phase 7 |
| Aug 2026 | CLI reconfigures stdout to UTF-8 | Windows cp1252 consoles crashed printing Unicode artifact content | Phase 6 fix |
| Aug 2026 | Cost-aware LLM routing (Phase 7.5) | Production cost optimization — cheapest healthy provider first, failover on 429s, tiered models, spend guard + tracking | Narendra |
| Aug 2026 | Multi-provider expansion planned (Phase 7.6) | Router is provider-agnostic; add OpenAI direct, Anthropic, Google Gemini as drop-in clients with price-tiered routing — never vendor-locked to Nvidia | Narendra |
| Aug 2026 | Persistence + auth planned (Phase 15) | Sessions must survive restarts (DB) and the dashboard must require login before public deployment | Narendra |
| Aug 2026 | LLM outputs scanned for jailbreak/refusal/executable echoes before parsing (advisory); strict schema validation is the enforcement gate | Defense-in-depth — a compromised model leaves a forensic trail but can never inject output | Phase 10 |
| Aug 2026 | Per-agent EWMA anomaly detection on response length + token usage (flags >50% token drops) | Catches degraded models early without hardcoding "normal" sizes | Phase 10 |
| Aug 2026 | Pricing is an immutable static catalog (MappingProxyType); agents reject unknown services and extra JSON keys | AI-poisoning defense — the model can never invent prices or smuggle fields | Phase 10 |
| Aug 2026 | Rate limiting enforced inside Orchestrator.run BEFORE any LLM call (global hourly + per-session in-flight gate) | Throttled analysis costs zero API credits; concurrent CLI/UI calls cannot interleave a session | Phase 10 |
| Aug 2026 | CLI sanitizes input identically to the dashboard (single input contract across entry points) | Audit found `<script>` could sit in CLI sessions; both UIs now enforce the same contract | Phase 10 fix |
| Aug 2026 | The 21 compliance rules moved to `cloudoptima/compliance/rules.py` as the single immutable source; the agent imports them and renders them into the prompt | One definition, three uses (module → prompt → validation) can never drift | Phase 8 |
| Aug 2026 | Compliance RAG uses ChromaDB when installed, else a deterministic keyword-overlap retriever over the same corpus | chromadb's `chroma-hnswlib` wheel fails to build on Windows without a C++ toolchain; the public API is identical either way, so the pipeline never breaks | Phase 8 |
| Aug 2026 | Pricing split into `static_db.py` (read-only lookup/estimate) + `azure_api.py` (live Retail Prices API, 1h cache, static fallback) | Free no-auth live prices when online; offline always works; static catalog stays the validation authority | Phase 8 |
| Aug 2026 | OpenAI (direct), Anthropic Claude, and Google Gemini added as routed providers; price table covers all 5 providers; `ROUTING_PROVIDERS` registry expanded | Never vendor-locked — cheapest healthy provider wins with automatic failover | Phase 7.6 |
| Aug 2026 | New providers use httpx (Nvidia pattern) or the existing openai SDK; Gemini key rides in a header, never in the URL | Consistent, testable clients; keys never leak into proxy/access logs | Phase 7.6 |

| Aug 2026 | RAG corpus is injection-scanned (indirect-injection defense); output-flagged responses (injection echo / executable / base64) are excluded from the LLM cache so a single poisoned response can't be replayed | Hostile documents and one-off poisoned model outputs must never propagate to other sessions | Phase 10 |
| Aug 2026 | RFC 0001: keep the custom orchestrator for now; revisit at deployment (MAF / Azure AI Foundry Agent Service) | Fixed linear 5-agent DAG, 478 tests, full control; framework value appears only when hosting agents in Foundry | Team + Punit (issue #6) |
| Aug 2026 | RFC 0001 gains a brainstorming & deliberation record (timeline, debate points, participants) | Punit asked whether we actually thought the framework through; the reasoning previously lived only in chat history, so the process is now documented alongside the conclusion | Narendra (issue #6 follow-up) |
| Aug 2026 | Azure AI Content Safety + Prompt Shields as an optional ML layer over the regex sanitizer, with graceful fallback (issue #2) | ML detectors beat regexes on harm + document attacks; same degrade-without-breaking contract as pricing | Team (issue #2) |
| Aug 2026 | Tool layer (issue #7): deterministic registry + FastMCP server + MCP bridge with registry fallback; tools are read-only and governed | Agents gain tool-driven capability without LLM-in-the-loop; MCP optional, never a hard dependency | Team (issue #7) |
| Aug 2026 | Action governance (issue #5): fail-closed policy checks with AGT delegation when installed, mirrored offline policy otherwise | Governance must not depend on an optional package; verdicts + audit trail identical either way | Team (issue #5) |
| Aug 2026 | Automated quality evaluation (issue #4) + red teaming (issue #3) ship as deterministic script harnesses with guarded optional SDKs | Eval/red-team must run without Azure keys or preview SDKs; azure-ai-evaluation and PyRIT activate when installed | Team (issues #3, #4) |
| Aug 2026 | Real-framework upgrades (audit follow-up): AGT `PolicyEngine` becomes the live runtime enforcer; Prompt Shields call the REST `text:shieldPrompt` endpoint; PyRIT campaign (`pyrit_redteam.py`) uses real target/converters/scorer/memory; eval adds always-on offline F1/Rouge | Punit asked for the Microsoft frameworks, not look-alikes; each integration degrades gracefully when its optional package/key is absent | Team (issues #2–#5) |
| Aug 2026 | Base64 decode-then-scan added to `sanitize.detect_injection` (recursive, sanitizer-category aware) after the PyRIT campaign surfaced short encoded payloads bypassing the 200-char blob heuristic | Real red teaming found a real gap; fixed at the sanitizer layer so every input path benefits | Team (issue #3 finding) |
| Aug 2026 | Phase 11 (Testing) closed in practice — 527 tests, 92.62% coverage (85% gate); per-file ≥90% on sanitize (99%), orchestrator (93%), agent_base (92%) | The original 30-test / 85% bar was exceeded long ago; the only leftover is recording a judge-model baseline once Azure OpenAI eval is configured | Team |
| Aug 2026 | Remaining roadmap sequenced: Phase 12 (Docker) → 13 (Azure deploy) → 14 (production) → 15 (persistence & auth); every PR gated on tests + `redteam --strict` + team/Punit review | Deployability first, then reliability, then data & auth — documented in `docs/PROGRESS_REPORT.md` | Team |
| Aug 2026 | `docs/PROGRESS_REPORT.md` published — issue resolutions, phase status, team contributions, and the remaining roadmap | One document answers Punit's review questions and onboards Andrew/Ivan on what remains | Narendra |
| Aug 2026 | External principal-engineer review: `Settings.__repr__` now shows `***REDACTED***` — never even the first 3 characters of a key | A leaked prefix narrows the search space for attackers reading logs/crash reports; masking is all-or-nothing | External review fix |
| Aug 2026 | External review: IaC backtick scanner only flags shell-looking content (operators/`$()`/command words), not Markdown inline code | v1 flagged every backtick span as command substitution — a false positive that withheld legitimate IaC artifacts | External review fix |
| Aug 2026 | External review: tool arguments validated against the declared parameter schema before execution; tools get a 15s execution timeout | A model passing `{"service": 42}` is rejected at the schema layer; a hung tool can never block the pipeline | External review fix |
| Aug 2026 | External review: severity-based routing (`severity_action`: pass / log / block / escalate) + `max_severity` on safety verdicts | Azure's 0-6 scale routes different severities differently — log vs block vs escalate, per Microsoft's documented pattern | External review fix |
| Aug 2026 | External review: ML safety layer is mandatory in production — `create_orchestrator` fails closed when `demo_mode=false` without Content Safety | Real LLM runs require the ML layer, not opt-in; demo mode keeps the regex floor and runs anywhere | External review fix |
| Aug 2026 | External review: error turns carry an `error_kind` taxonomy (llm / parse / validation / prompt_build); orchestrator audits failed turns with the reason | Distinguishes a transient provider outage from a bad model response — different retry/escalation paths | External review fix |
| Aug 2026 | External review: PyRIT campaign hardened with Flip + ROT13 converters — they exposed 3 NEW bypasses (jailbreak/role-switch/RAG-poison under Flip, ROT13, and flip-or-ROT13-of-base64) | Closed with involution unscrambling (`sanitize.obfuscated_forms` + `decoded_base64_forms`): decode the scramble to restore the attack; campaign now 75 variants → 0.0% ASR | External review → real red-team finding |
| Aug 2026 | External review: eval harness gains a `--fail-under` CI gate and a version-pinned judge model (`AZURE_OPENAI_EVAL_MODEL`) | Quality regressions block deployment instead of hiding in a JSON report; pinned judge prevents phantom score drift | External review fix |
| Aug 2026 | External review: Dockerfile (multi-stage, non-root, healthcheck) + `.dockerignore` + `.github/workflows/ci.yml` (ruff · mypy · pytest · red-team strict · PyRIT · eval · AGT lint · MCP smoke; Azure deploy on main) | Container packaging and CI/CD were the missing operational pieces between "demo" and "deployable" | External review fix |
| Aug 2026 | External review: auth scaffold for Phase 15 — `AUTH_*` config + dashboard login gate (Streamlit native OIDC with Entra ID, or App Service Easy Auth) | "Anyone who can reach the dashboard can burn LLM credits" was the #1 production risk; gate is a no-op until enabled | External review fix |
| Aug 2026 | External review (round 2): Bidi control chars stripped in `clean_input`/`clean_output` — the reviewer's P0 (an RLO makes a backwards payload render as an instruction; NFKC preserves it, so regex stripping is mandatory) | U+202E etc. are not C1 controls; the only correct fix is explicit removal before any pattern scan | External review P0 |
| Aug 2026 | External review (round 2): `_ATBASH_TABLE` now complements digits (0↔9) exactly like PyRIT's AtbashConverter, and `obfuscated_forms` is closed under 2 transform rounds | atbash(b64(x)) stays valid base64 charset but decodes to binary; only the involutive re-scan restores the real payload | External review → real red-team finding |
| Aug 2026 | External review (round 2): leetspeak folds now cover symbols (c→() and both ambiguous `1` resolutions; key injection phrases get whole-phrase leet-tolerant regexes; the offline harm floor matches "k111 411 u53r5" | A global digit fold can never recover words that mix i and l ("kill", "helpful assistant"); phrase-level classes keep precision — a lone "kill" is still ops language | External review → real red-team finding |
| Aug 2026 | External review (round 2): leet-of-base64 marked a documented **known gap** in the PyRIT campaign (`KNOWN_GAP_VARIANTS`) — reported, excluded from the strict gate, closed by the mandatory ML Content Safety layer | leet(b64(x)) is not deterministically decodable by any reader; pretending otherwise would be theater, and the reviewer accepted 95% coverage | Team — same policy as deterministic harness |
| Aug 2026 | Reviewer's P1 (async pipeline) and P2 (Redis rate limits) initially deferred to Phase 14/15 — then **done in round 3** (see below) once the security rounds closed out | The reviewer's round-3 re-review made them the explicit homework for a 10/10; deferring had been the right call while security work was open | External review (deferred → done) |
| Aug 2026 | External review (round 3, P1): `BaseAgent.analyze` and `Orchestrator.run` are now real coroutines; every LLM client gained `agenerate()` (httpx.AsyncClient / AsyncAzureOpenAI / AsyncOpenAI / async sleep for Mock); `agenerate_with_retry` awaits the backoff; the three specialists that only depend on the architect run concurrently via `asyncio.gather` | "Synchronous LLM pipelines are an instant PR rejection" — LLM calls are 99% network I/O, so serializing them capped throughput; concurrency is proven by a peak-concurrency test (all three overlap) | External review P1 |
| Aug 2026 | External review (round 3, P2): `RateLimitStore` protocol + `MemoryRateLimitStore` (default) + `RedisRateLimitStore` (INCR/EXPIRE, lazy import, injectable client) behind one `RateLimiter`; `Settings.rate_limit_backend` (`memory`/`redis`) + `redis_url`; orchestrator gets its limiter injected | An in-memory dict makes the "60/hour" quota 180/hour across 3 workers; the interface lets a scale-out deployment share one Redis counter | External review P2 |
| Aug 2026 | External review (round 3, P3): new `cloudoptima/context.py` — `AppContext` owns settings, llm client, audit logger, anomaly detector, and rate limiter; `Orchestrator.from_settings` builds it and injects the instances into every agent (`context=` kwarg); module getters remain only as a fallback for direct construction | Hidden module globals made tests bleed state and blocked multi-tenant isolation; a context container passes dependencies explicitly and verifiably | External review P3 |
| Aug 2026 | Sync callers bridged: CLI (`app.main`), the Streamlit background thread, and the eval script all wrap the async `run` with `asyncio.run`; the dashboard captures the orchestrator reference before the thread starts | `asyncio.run` in a non-main thread works fine here (verified), but the lambda must bind the orchestrator eagerly — st.session_state may be gone when the thread actually executes | Team (round-3 integration) |